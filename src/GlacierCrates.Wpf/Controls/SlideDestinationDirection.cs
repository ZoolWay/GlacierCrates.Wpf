using System;

namespace GlacierCrates.Wpf.Controls
{
    /// <summary>
    /// Defines a slide destination direction, i.e. in which direction the slide 'goes'.
    /// </summary>
    public enum SlideDestinationDirection
    {
        None,
        Up,
        Down,
        Left,
        Right,
    }
}
